/*
 User Registers → Store hashed password in the database
2️⃣ User Logs In → Verify hashed password
3️⃣ Issue JWT → If correct, generate and send a JWT
4️⃣ Subsequent Requests → Use JWT for authentication
 */

